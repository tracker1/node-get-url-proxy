{
  "name": "get-url-proxy",
  "version": "1.0.0",
  "description": "get-url-proxy will return the applicable proxy to use for a given URL based on NO_PROXY and PROT_PROXY environment variables",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tracker1/node-get-url-proxy.git"
  },
  "keywords": [
    "proxy",
    "environment",
    "url"
  ],
  "author": "Michael J. Ryan",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tracker1/node-get-url-proxy/issues"
  },
  "homepage": "https://github.com/tracker1/node-get-url-proxy#readme",
  "dependencies": {
    "no-proxy": "^1.0.2"
  },
  "optionalDependencies": {
    "url-parse": "^1.2.0"
  },
  "scripts": {
    "publish-major": "npm version major && git push origin master && git push --tags",
    "publish-minor": "npm version minor && git push origin master && git push --tags",
    "publish-patch": "npm version patch && git push origin master && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
